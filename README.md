# bearbash
  * automated shell commands, written in vim, run in bash
  * download respective sample `.log`, .`txt` or other files to run the scripts if necessary <br>

# how to run?
<sub><b>bash</b><i>&emsp;(replace `folder` with respective directory, replace `script` to the name of the script)</i></sub>
```
$ cd /folder
$ chmod +x script.sh
$ ./script.sh
```
<p align="center"><sup><i>"how do i close vim?"</i></sup><br></p>
